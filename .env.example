# =============================================================================
# StudyFlow - Environment Variables Template
# Copy this file to .env.local and fill in your values for development
# For production, set these environment variables in your deployment platform
# =============================================================================

# CONVEX BACKEND CONFIGURATION
# ==============================
# Convex deployment identifier (e.g., dev:deployment-name)
# Get this from `npx convex deploy` or Convex dashboard
CONVEX_DEPLOYMENT=dev:careful-cat-984

# Convex API URL for the frontend
# Format: https://{deployment-id}.convex.cloud
VITE_CONVEX_URL=https://careful-cat-984.convex.cloud

# Convex site URL (ends in .site for browser-based queries)
# Format: https://{deployment-id}.convex.site
VITE_CONVEX_SITE_URL=https://careful-cat-984.convex.site

# FRONTEND CONFIGURATION
# =======================
# Your application's public URL
# Used for WorkOS OAuth redirects
VITE_SITE_URL=http://localhost:3000

# WORKOS AUTHENTICATION
# ======================
# WorkOS Client ID (get from WorkOS dashboard)
# Used for SSO and user management
WORKOS_CLIENT_ID=client_01XXXXXXXXXXXXXXXXXXXXXXXXXX

# WorkOS API Key (get from WorkOS dashboard)
# NEVER commit this to version control!
# Mark as secret in your deployment platform
WORKOS_API_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Cookie encryption password for WorkOS session management
# Generate a random 32-character string using: openssl rand -base64 24
# NEVER commit this to version control!
# Mark as secret in your deployment platform
WORKOS_COOKIE_PASSWORD=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OAuth redirect URI after WorkOS login
# Must match your WorkOS dashboard configuration
WORKOS_REDIRECT_URI=http://localhost:3000/callback

# NODE ENVIRONMENT
# =================
# Set to 'production' for production builds
# Used for error handling and optimization decisions
NODE_ENV=development
